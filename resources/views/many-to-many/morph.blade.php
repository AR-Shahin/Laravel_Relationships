<x-guest-layout>

    <div class="p-10">
    <h1 class="text-center text-purple-800 text-3xl font-bold">Many to Many Polymorphic</h1>
    <hr>

    {{-- <h1>{{ $post->title }}</h1>
    @foreach ($post->reviews as $review)
    <p>Review <b>({{ $loop->index + 1 }})</b> : {{ $review->body }}</p>
    <p>Status : {{ $review->is_active }}</p>
    <hr class="border">
    @endforeach --}}


    </div>
</x-guest-layout>
